# ---------------------------------------------------------------- UOTP API
# Required. Without it the bot refuses to start rather than failing later
# with a confusing 401 on the first order.
UOTP_API_KEY=
UOTP_BASE_URL=https://uotp.store

# UOTP does not publish API documentation, so the endpoint paths and the JSON
# key names are configurable. Point these at your real account's API and the
# adapter works without code changes. `python -m uotpbot.cli` will report a
# clear error naming the key it could not find if a mapping is wrong.
UOTP_AUTH_HEADER=Authorization
UOTP_AUTH_SCHEME=Bearer
UOTP_BALANCE_PATH=/api/v1/balance
UOTP_PRICES_PATH=/api/v1/prices
UOTP_BUY_PATH=/api/v1/number
UOTP_SMS_PATH=/api/v1/sms
UOTP_CANCEL_PATH=/api/v1/cancel
UOTP_TIMEOUT=20

# Response field mapping (dotted paths allowed, e.g. data.wallet.balance)
UOTP_FIELD_BALANCE=balance
UOTP_FIELD_ORDER_ID=id
UOTP_FIELD_PHONE=number
UOTP_FIELD_PRICE=price
UOTP_FIELD_SMS_LIST=messages
UOTP_FIELD_SMS_TEXT=text
UOTP_FIELD_SMS_SENDER=sender
UOTP_FIELD_SMS_TIME=created_at
UOTP_FIELD_PRICES=prices

# ------------------------------------------------------------------- fees
# Payment gateway charge on a sale. UPI is near zero; cards are ~2%.
FEE_GATEWAY_RATE=0.02
FEE_GATEWAY_FIXED=0
# GST charged on the gateway's own fee.
FEE_GATEWAY_GST=0.18
# Output GST on your sales. 0.18 once GST-registered, 0 below the threshold.
FEE_GST_RATE=0
# True = your advertised price already includes GST. False = GST is added on top.
FEE_GST_INCLUSIVE=true
# Share of delivered orders customers dispute anyway. Pure loss: you refund
# the sale but the number is already consumed.
FEE_CHARGEBACK_RATE=0

# ----------------------------------------------------------------- engine
# Max numbers bought for one order. Costs rise linearly with this, and it does
# NOT lower cost per delivery -- see tests/test_economics.py.
ENGINE_RETRY_CAP=3
# UOTP refunds only if no OTP arrives within 5 minutes, so do not wait longer
# than that or you forfeit the refund.
ENGINE_OTP_TIMEOUT=290
ENGINE_POLL_INTERVAL=3
ENGINE_AUTO_REFUND=true
# Top the provider wallet up to this multiple of the order price when empty.
ENGINE_TOPUP_HEADROOM=5
ENGINE_DEFAULT_COUNTRY=in

# ---------------------------------------------------------------- telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_ID=
# Comma-separated Telegram user IDs allowed to use the bot. Empty = anyone.
TELEGRAM_ALLOWED_USERS=

# ------------------------------------------------------------------ storage
LEDGER_PATH=ledger.db
# Leave unset to use the bundled data/uotp_prices.csv.
PRICES_PATH=
